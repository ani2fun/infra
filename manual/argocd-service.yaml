apiVersion: v1
kind: Service
metadata:
  name: svc-np-argocd
spec:
  type: NodePort
  selector:
    app: argocd-server
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
      nodePort: 30001
