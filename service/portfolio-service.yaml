apiVersion: v1
kind: Service
metadata:
  name: portfolio-app
spec:
  type: NodePort
  selector:
    app: portfolio-app
  ports:
    - port: 80
      protocol: TCP
      targetPort: 80
      nodePort: 30000
