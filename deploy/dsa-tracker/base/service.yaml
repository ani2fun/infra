apiVersion: v1
kind: Service
metadata:
  name: dsa-tracker
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dsa-tracker
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
