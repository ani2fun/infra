apiVersion: v1
kind: Service
metadata:
  name: dsa-tracker-frontend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: dsa-tracker
    app.kubernetes.io/component: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
