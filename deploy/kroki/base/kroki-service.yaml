apiVersion: v1
kind: Service
metadata:
  name: kroki
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kroki
    app.kubernetes.io/component: server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
