apiVersion: v1
kind: Service
metadata:
  name: notebook-app
  namespace: apps
  labels:
    app.kubernetes.io/name: notebook-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: notebook-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP