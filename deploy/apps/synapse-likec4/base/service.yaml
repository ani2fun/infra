apiVersion: v1
kind: Service
metadata:
  name: synapse-likec4
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: synapse-likec4
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
