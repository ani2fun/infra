apiVersion: v1
kind: Service
metadata:
  name: cortex-tutor
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: cortex-tutor
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
