apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bytebase

resources:
  - ../../base
  - ingress.yaml
  # Both produced by scripts/secrets/seal-bytebase-secrets.sh.
  - sealedsecret-oauth2-proxy.yaml
  - sealedsecret-db.yaml
