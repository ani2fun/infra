---
# PLACEHOLDER — replace by running scripts/secrets/seal-synapse-secrets.sh.
# A sealed COPY of the Keycloak bootstrap-admin credentials for apps-prod: the
# canonical secret lives in the `identity` namespace (keycloak-admin-secret),
# and a pod cannot reference a Secret across namespaces. Synapse's
# account-deletion adapter (step 21) authenticates with it against the Admin API.
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: synapse-keycloak-admin
  namespace: apps-prod
spec:
  encryptedData:
    username: CHANGE_ME_RUN_seal-synapse-secrets.sh
    password: CHANGE_ME_RUN_seal-synapse-secrets.sh
  template:
    metadata:
      name: synapse-keycloak-admin
      namespace: apps-prod
