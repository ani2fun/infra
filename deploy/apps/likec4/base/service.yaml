apiVersion: v1
kind: Service
metadata:
  name: likec4
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: likec4
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
