# Intel PCH TCO hardware watchdog, so systemd can arm it (see systemd/10-watchdog.conf).
# Added 2026-07-18 after wk-1 died with no panic, no clean shutdown and no ACPI power event,
# then stayed down ~96 minutes because nothing on the box could reset it.
iTCO_wdt
