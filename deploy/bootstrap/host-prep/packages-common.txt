# Packages installed on every node.
# Comment lines (starting with #) and blank lines are ignored by prepare-host.sh.
# Captured from the apt-mark showmanual intersection across all four nodes.

# Networking + WireGuard
wireguard
wireguard-tools

# Container/Kubernetes packet filtering toolchain
iptables
iptables-persistent
nftables
conntrack
ebtables
ipset

# Base CLI utilities
curl
jq
socat
ca-certificates
gnupg
dnsutils
htop
tcpdump

# Security
fail2ban
unattended-upgrades
