apiVersion: v1
kind: Service
metadata:
  name: piston
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: piston
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 2000
      targetPort: 2000
      protocol: TCP
