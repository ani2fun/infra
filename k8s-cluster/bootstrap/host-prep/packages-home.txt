# Additional packages installed on home nodes (ms-1, wk-1, wk-2).
# Comment lines and blanks are ignored.

# Hardware enablement kernel (currently 6.17.x)
linux-generic-hwe-24.04

# Operator convenience
tree
nmap

# ms-1 specifically also benefits from:
# (the script reads packages-ms-1.txt if present; otherwise these are common
# enough to include here)
helm
git
gpg
unzip

# Notes:
# - ubuntu-desktop-minimal is currently installed on all three home nodes.
#   It is NOT listed here because it is not required for cluster operation.
#   Install it separately if you want the GUI.
# - chronyd is installed only on wk-2 (handled by prepare-host.sh based on
#   hostname). systemd-timesyncd is the default on ms-1 and wk-1.
# - ollama is installed only on wk-1 (operator preference, not Kubernetes-
#   managed). Install separately from ollama.com after host prep.
